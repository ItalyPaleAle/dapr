apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: actorstore
spec:
  type: actorstore.sqlite
  version: v1
  metadata:
    - name: connectionString
      value: "file::memory:"
      # To test using a database on disk
      #value: "file:test.db"
